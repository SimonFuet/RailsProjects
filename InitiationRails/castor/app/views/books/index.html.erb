<%= image_tag "books.jpeg", width: 64, height: 64 %><h1>Liste des livres</h1>
<%= paginate @books %>
<% @books.each do |book| %>
<p>
  <%= book.id %> : <%= book_link book %> 
  <% if book.category %>
    (<%= book.category.title %>)
  <% end %>
</p>
<% end %>

<%= render "books/form" %>
